loader=loader, extraction_chain=extraction_chain, comparison_chain=comparison_chain, retriever=retriever, duplicate_detector=duplicate_detector, report_generator=report_generator, graph=StateGraph(AppState, chunks=self.loader.load(pdf_path, compiled=self.build(, AppState={, invoice_metadata=self.extraction_chain.extract(state["chunks"], line_items=state["line_items"], query=state["invoice_metadata"].get("invoice_number", retrieved=self.retriever.retrieve(query, discrepancies=state["discrepancies"], retrieved_documents=state["retrieved_documents"], invoice_number=state["invoice_metadata"].get("invoice_number", is_duplicate=self.duplicate_detector.is_duplicate(invoice_number, report=self.report_generator.generate(