quotes=state.get("parsed_quotes", alignments=state.get("category_alignments", category_totals=self._compute_category_totals(quotes, outliers=[], scope_gaps=self._detect_scope_gaps(category_totals, comparison_results={, mapping={}, bid_label=quote.source_key, desc_key=(item.description or ""), category_id=getattr(item, amount=item.amount, categories=set(, values=[], sorted_vals=sorted(values, n=len(sorted_vals, median==, 2==, gaps=[], bids=list(category_totals.keys(, present_in=[bid for bid in bids if category in category_totals.get(bid, {})], absent_in=[bid for bid in bids if category not in category_totals.get(bid, {})]